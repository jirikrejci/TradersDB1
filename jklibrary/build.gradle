apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    sourceCompatibility = 1.7
    targetCompatibility = 1.7
    compile files('libs/JKNetworkingLibrary_jar/JKNetworkingLibrary.jar')
}